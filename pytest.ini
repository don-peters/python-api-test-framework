# Pytest configuration for comprehensive test execution and reporting
[tool:pytest]
addopts = --maxfail=5 --disable-warnings -q --tb=short --html=report.html --self-contained-html
testpaths = tests
markers =
    smoke: Quick smoke tests to check basic functionality
    regression: Comprehensive regression tests
    integration: Tests for integration between components
